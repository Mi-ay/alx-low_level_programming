#include "main.h"
/**
 * print_alphabet - a function that prints alphabet in lower case
 * Return: 0(success)
 */
void print_alphabet(void)
{
	char o;

	for (o = 'a' ; o <= 'z' ; o++)
		_putchar(o);
	_putchar('\n');
}
